# Очередь человека

Здесь только то, что агент сделать не может: действия от твоего имени, оплаты
и данные, которых нет в репозитории. Всё остальное агент делает сам.

Правило: если пункт можно переписать так, чтобы его выполнил агент, — он не
должен здесь лежать. Сделанное отмечай `[x]`, агент уберёт его в журнал.

---

## Что делать сейчас, 2026-09-13 · порядок важен

Первые два пункта — сегодня, оба упираются в задержку: они запускаются и
дальше зреют сами, пока идут остальные дни. Третий и четвёртый — завтра.

День 1 дороже остальных: суммарно около сорока минут. Дальше — минуты.

- [ ] **1. Скаут: войти в 2–3 чата и завести доступы.** Это гейт на лучший
      канал лидов, а без лидов платёжные рельсы не нужны вовсе. Вступать надо
      самому и не за один день, поэтому начинать сегодня.
      Нужно: `my.telegram.org` → API development tools даёт `SCOUT_API_ID` и
      `SCOUT_API_HASH`; затем `npm run login` один раз с телефоном под рукой —
      печатает `SCOUT_SESSION`; и список чатов в `SCOUT_CHATS`.
      Аккаунт отдельный, не основной. Назови чаты или скажи, в каких уже
      состоишь — угадывать хэндлы за тебя я не стану, неверный потратит твоё
      время вернее, чем вопрос.

- [ ] **2. Payoneer: начать регистрацию.** Не потому что он блокирует —
      Upwork умеет платить и на местный банк, а юрлицо примет SWIFT. Дело в
      сроке: верификация идёт днями, а деньги нужны внутри двух недель.
      Запустить сегодня и забыть. Заодно скажи, есть ли уже аккаунты
      Payoneer и Upwork — если есть, пункт закрыт.
      Решение по рельсам целиком в `experiment/PAYMENTS.md`, выбирать заново
      не надо.

- [ ] **3. Домен** (завтра). Сайт живёт на `devuz.maximov-tech.ru` — субдомен
      чужого хоста, для продажи проекта за $2500 читается как тестовая
      страница. От тебя регистрация и оплата, ~$15–50 из бюджета. Дальше DNS,
      nginx, certbot, редиректы и канонические ссылки — моё. Напиши, какой
      купил.

- [ ] **4. Реквизиты** (завтра). Агент их не выдумает: точный адрес офиса
      (или «работаем удалённо» — это честнее пустышки, и разметку тогда я
      уберу), название юрлица и ИНН, почта для счетов, Telegram для ответов
      клиентам.

## Сделано

Пока пусто.
